"""Compatibility shim for the former external skill-cloning dependency."""

def add_skill_cloning_to_path(path=None) -> str:
    """Deprecated; the core is bundled in this standalone package."""
    return ""
